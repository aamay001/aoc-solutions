const fileErrorMessage: string = 'File loaded but something is wrong with your data 🤷‍♂️';
const dataFileInstructions: string = `Input data must be in a .txt file.
Line endings should be LF.
File should be formatted as UTF-8.`;
const noDataMessage: string = 'No data loaded.';
const appImageAlt: string = 'A gold star inside of two brackets like a block of code. The app image for this app.';

export {
  fileErrorMessage,
  dataFileInstructions,
  noDataMessage,
  appImageAlt,
};